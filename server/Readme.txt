1. Go inside server
   npm install
2. go inside client
   npm install
3. from the path under server folder
  npm run dev
Application should automatically open up in the Browser

source of data:
its under /server/routes/subRoutes
varibale name: values

Future Step:
Data on boarding:
Option 1: Get the data by url
Option 2: Get the data from reading a file(Example: Select a file)
Option 3: Find an automated way to read a file in a static locaiton
